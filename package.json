{
  "name": "ghl-mcp-server",
  "version": "1.0.0",
  "description": "GoHighLevel MCP Server on Cloudflare Workers - Full GHL API access for AI Agents",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "dependencies": {
    "agents": "^0.0.98",
    "@anthropic-ai/sdk": "^0.39.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250124.0",
    "typescript": "^5.7.3",
    "wrangler": "^3.101.0"
  }
}
